cmake_minimum_required(VERSION 3.13)

project(random_walk)

set(CMAKE_CXX_STANDARD_11)

add_subdirectory("dependencies/raylib")

set(CMAKE_MSVC_RUNTIME_LIBRARY "MultiThreadedDebug$<$<CONFIG:Debug>:Debug>") # Links cruntime library statically
set(CMAKE_MSVC_RUNTIME_LIBRARY "MultiThreaded$<$<CONFIG:Release>:Release>")



file(GLOB_RECURSE MY_SOURCES CONFIGURE_DEPENDS "${CMAKE_CURRENT_SOURCE_DIR}/src/*cpp")

add_executable(random_walk ${MY_SOURCES})

target_include_directories(random_walk PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/dependencies/include/")
target_link_libraries(random_walk PRIVATE raylib)